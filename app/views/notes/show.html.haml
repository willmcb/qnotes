.col-md-8.offset-2
  %h4= @note.title
  %p= markdown(@note.body)
  %p
    %small
      Tags: #{raw @note.tags.map { |tag| link_to tag.name, tag_path(tag.id) }.join(', ')}
  %p
    %small
      Collection: #{link_to @note.collection.name, collection_path(@note.collection)}
